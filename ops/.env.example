DB_USER=username
DB_PASSWORD=password
DB_NAME=box-db
DB_PORT=5432
# Inside the docker network — consumed by bff, keycloak, etc.
DB_URI="postgresql://${DB_USER}:${DB_PASSWORD}@postgres:${DB_PORT}/${DB_NAME}"
